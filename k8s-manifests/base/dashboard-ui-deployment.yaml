apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard-ui-deployment
  labels:
    app: dashboard-ui
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dashboard-ui
  template:
    metadata:
      labels:
        app: dashboard-ui
    spec:
      containers:
      - name: dashboard-ui-container
        image: dongju101/dashboard-ui:v1.0
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: http
        env:
        - name: SERVICE_NAME
          value: "dashboard-ui"
        - name: SERVICE_PORT
          value: "80"
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        volumeMounts:
        - name: nginx-config
          mountPath: /etc/nginx/conf.d/default.conf
          subPath: default.conf
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: nginx-config
        configMap:
          name: dashboard-ui-nginx-config